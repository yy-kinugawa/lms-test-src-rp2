<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="jp.co.sss.lms.mapper.MCodeMapper">

	<!-- Task.51 -->
	<select id="findByKey" resultType="jp.co.sss.lms.dto.CodeDto">
		SELECT * FROM m_code
		WHERE key = #{key}
		ORDER BY "order" ASC
	</select>

</mapper>