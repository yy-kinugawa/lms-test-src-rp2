<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="jp.co.sss.lms.mapper.MCategoryMapper">

	<!-- Task.115 -->
	<select id="getCategoryDtoList" resultType="jp.co.sss.lms.dto.CategoryDto">
		SELECT
			category_id,
			category_name,
			category_description,
			course_id
		FROM m_category
		WHERE delete_flg = #{deleteFlg}
	</select>

</mapper>